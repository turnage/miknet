CMAKE_MINIMUM_REQUIRED(VERSION 2.6)
PROJECT(Tubuil)

INCLUDE_DIRECTORIES(src src/include)
FILE(GLOB SRC_TUBUIL src/*.c)

ADD_EXECUTABLE(test ${SRC_TUBUIL})

SET(CMAKE_C_WARNINGS "-Wall")

SET(CMAKE_C_FLAGS "${CMAKE_C_WARNINGS} ${CMAKE_C_FLAGS}")
